"use client";import{useEffect,useState}from"react";import{useParams,useRouter}from"next/navigation";import{SpaceForm}from"@/components/space/SpaceForm";import{SpaceAnalysisPanel}from"@/components/space/SpaceAnalysisPanel";import type{Space}from"@/types/space";export default function Page(){const{id}=useParams<{id:string}>(),router=useRouter(),[space,setSpace]=useState<Space|null>(),[error,setError]=useState("");useEffect(()=>{fetch(`/api/spaces/${id}`).then(async r=>{if(r.status===404)return router.replace("/dashboard/spaces");if(!r.ok)throw new Error();setSpace(await r.json())}).catch(()=>setError("공간 정보를 불러오지 못했어요."))},[id,router]);if(error)return <section className="section"><div className="container empty">{error}</div></section>;if(!space)return <section className="section"><div className="container">공간 정보를 불러오는 중...</div></section>;return <section className="section"><div className="container" style={{maxWidth:1000}}><p className="eyebrow">Edit space</p><h1 className="section-title">공간 수정</h1><SpaceForm space={space}/><SpaceAnalysisPanel spaceId={space.id}/></div></section>}
