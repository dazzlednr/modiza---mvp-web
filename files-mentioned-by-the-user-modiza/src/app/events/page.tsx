import Link from"next/link";import{communities}from"@/data/communities";import{CalendarDays,MapPin}from"lucide-react";export default function Page(){return <section className="section"><div className="container"><p className="eyebrow">This week</p><h1 className="section-title">이번 주 참여 가능한 모임</h1><div className="grid" style={{marginTop:30}}>{communities.slice(0,8).map((c,i)=><Link className="panel" href={`/communities/${c.slug}`} key={c.id} style={{display:"flex",gap:20,alignItems:"center"}}><div style={{minWidth:70,textAlign:"center"}}><b style={{fontSize:28}}>{15+i}</b><div className="muted">7월</div></div><div><span className="tag">{c.category}</span><h3>{c.name}</h3><div className="meta"><CalendarDays size={14}/>19:30 <MapPin size={14}/>{c.region}</div></div></Link>)}</div></div></section>}
