import Link from"next/link";import{CalendarDays,MapPin}from"lucide-react";import{createServerSupabaseClient}from"@/lib/supabase/server";import{listCommunities}from"@/repositories/communityRepository";
export const dynamic="force-dynamic";
export default async function Page(){const communities=await listCommunities(createServerSupabaseClient());return <section className="section"><div className="container"><p className="eyebrow">This week</p><h1 className="section-title">이번 주 참여 가능한 모임</h1><div className="grid" style={{marginTop:30}}>{communities.slice(0,8).map(c=><Link className="panel" href={`/communities/${c.slug}`} key={c.id} style={{display:"flex",gap:20,alignItems:"center"}}><div><span className="tag">{c.category}</span><h3>{c.name}</h3><div className="meta"><CalendarDays size={14}/>{c.nextMeetingAt?new Date(c.nextMeetingAt).toLocaleString("ko-KR",{dateStyle:"medium",timeStyle:"short"}):"일정 준비 중"}<MapPin size={14}/>{c.mainRegion}</div></div></Link>)}</div></div></section>}
